import { SubProjectsOfProjectPipe } from './sub-projects-of-project.pipe';

describe('SubProjectsOfProjectPipe', () => {
  it('create an instance', () => {
    const pipe = new SubProjectsOfProjectPipe();
    expect(pipe).toBeTruthy();
  });
});
